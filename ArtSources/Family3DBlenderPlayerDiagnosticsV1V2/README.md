# Player Blender diagnostics V1/V2

Status: `REJECTED / DO_NOT_IMPORT / DO_NOT_REUSE_AS_SUCCESS`

These files preserve the exact tested V1 and V2 Blender attempts so another workstation can see
what failed. Neither model is a candidate, donor, base mesh, or production asset.

- V1: rigid crown hair, stacked-box torso, ankle/shoe gaps, pelvis overlap, generic identity.
- V2: sausage-like hair locks, inflated egg-shaped hoodie, pasted-on eyes, generic silhouette.

The matching scripts are under `Tools/Blender/`. Start a genuinely new version from the locked
turnaround and the failure notes; do not make a minor patch and rename either diagnostic as a pass.
