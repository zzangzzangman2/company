# Superseded V3 internal runs

Only these are the current V3 candidate pair:

- `BuildRun3`
- `D3D11QaRun3Visible`

`Build`, `BuildFinal`, and `D3D11QaRun1Visible` are retained only as internal diagnostics. Their
first-frame and receipt contracts were superseded before delivery. Do not package or run them.
