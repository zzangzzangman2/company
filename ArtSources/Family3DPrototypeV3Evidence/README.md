# Family 3D Motion Lab V3 — checked-in evidence

Status: `MOTION_PROXY_PASS / FINAL_FAMILY_IDENTITY_NOT_ELIGIBLE`

This directory is the compact, clone-safe evidence set for the visible D3D11 V3 locomotion run.
The Styloo actors in the images/video are CC0 motion proxies, not the final Player, Father, Mother,
or Older Sister meshes.

Included:

- automatic QA receipt and frame metadata;
- one original-resolution 24-pose close-up sheet for each of SW/NW/NE/SE;
- turn sequence sheet and synchronized several-cycle MP4;
- visual review, production-isolation audit, reference-video/build report;
- standalone build tree manifest and manifest receipt.

Excluded as reproducible generated output:

- 417 raw PNG capture frames;
- the 729 MB Windows build tree;
- Unity build logs.

Reproduce the build with the command in
`Docs/FAMILY_3D_CHARACTER_HANDOFF_2026-08-24.md`. Recreate contact sheets/video after a fresh capture
with `Tools/Build-Family3DMotionLabEvidenceV3.ps1 -QaRoot <capture-folder>`.
