# 첫 자체 제품: 하청 → 개발 → 판매 → 유지보수

2026-09-07 `fc-win-20260907.1`로 공개했다. 게임 소스는 `c0709823c0e45c4152c673ca0b67d7a1e1506bc7`이다.
[실제 Release/패치 검증](Evidence/StarterBusinessRelease20260907/README.md). 기존 메인 EXE/사용자 캐시/세이브는 보존했다.

## 플레이 흐름

`사업 → 자체 제품`에서 첫 제품 **우리가게 관리 프로그램**을 진행한다.

1. 시작 자금 500만 원, 빈 사무실과 가족 4명은 유지한다. 상점에서 책상·PC·의자 세트를 40만 원에
   구매해 배치한다. 엄마·누나는 현재 승인된 아빠·아들 3D 임시 표시를 계속 사용한다.
2. `타자 연습 프로그램 단어 DB 입력` 하청을 수락하고 실제 책상에서 끝낸다.
3. `비디오·만화책 대여점 연체 관리` 하청을 수락하고 끝낸다. 첫 수락 후 일반 게시판이 바뀌어도
   이 두 입문 계약은 자체 제품 화면에 고정 안내된다. 실패 시 기존 정산을 재사용하지 않고 새 ID로 재수주한다.
4. 두 계약의 완료 경험이 있으면 **30만 원**을 지불해 자체 개발을 시작한다. **24인시 / 30일**의
   개발·시험 업무를 가족에게 배정해야 한다. 달력만 넘기거나 UI를 열어 둔다고 개발되지는 않는다.
5. 완료 후 `시험 판매 시작`을 누르면 고객 3곳, 사용권 매출 18만 원을 정확히 한 번 받는다.
6. 매주 `이번 주 유지보수 접수` 후 **2인시**를 실제로 일한다. 첫 판매부터 7일 단위의 정산 시각까지
   완료해야 고객당 **2만 원**의 유지보수 요금이 들어온다.

가족 배정은 한 번에 최대 4인시다. 완료 후 다시 배정한다. 플레이어 버튼은 작업 대상을 선택할 뿐
자동으로 진척을 주지 않는다. 사무실로 돌아가 내 책상의 접근 칸에서 **E를 유지**해야 한다.
다른 가족은 기존 이동·예약·착석·업무 coordinator를 통과한다. 학교·외부 일정·체력·좌석 부족·이미 작업 중인
경우의 거부 사유를 그대로 알린다. 저장 후 장기 진행은 유지되지만 이동/배정 명령은 다시 내린다.

초기 상점은 책상 세트만 판매하므로 정수기·휴게석이 아직 없다. 실제 사무실 밖에서 일정상
23:00~07:00 수면을 취하면 8게임시간당 체력 한 바를 회복한다. 일반 OffDuty·사무실 근무·벽시계
경과만으로 회복하지 않는다. 소진된 가족을 계속 배정하지 말고 다음 날 회복 후 업무를 분담한다.

이 입문 두 계약과 자체 개발/지원은 PC만 쓰는 업무다. 초기 상점에 없는 회의탁자/프린터 때문에
첫 계약이 막히지 않는다. 다른 기존 계약의 장비 필요 조건을 전부 없앤 것은 아니다.

## 잠정 밸런스와 결과

| 항목 | 초기값 |
| --- | --- |
| 개발 투자 / 필요 작업 / 마감 | 300,000원 / 24인시 / 30일 |
| 첫 시험 고객 / 사용권 | 3곳 / 곳당 60,000원 |
| 주간 유지보수 / 고객당 요금 | 2인시 / 20,000원 |
| 고객 수 상한 | 8곳 |
| 유지보수 성공 | 만족도 +10, 기존 고객 요금 정산 |
| 좋은 제품의 고객 증가 | 품질 ≥55, 만족도 ≥60이면 성공 주에 1곳 추가 + 사용권 매출 |
| 유지보수 미완료 | 그 주 요금 0원, 만족도 -20, 고객 1곳 이탈(최소 0) |
| 고객 0곳에서 회복 | 다음 주 유지보수를 완료하면 1곳 재유치 + 사용권 매출 |

신규 수치는 사용자가 확정한 500만 원/40만 원과 달리 **플레이테스트용 제안값**이다. 유지보수 마감과
정산 시각은 동일한 정수 GameTime이다. 마감 정각 완료를 허용하기 위해 정산은 그 다음 게임 분에 처리한다.
매출은 개발비·인건비 등을 뺀 순이익이 아니다. 이 버전은 별도 유료 광고/서버비/고용비를 새로 차감하지 않는다.

## 기술·실적 정산 수정

- 기술 보상은 UI의 player work session에서 제거하고 `ContractPortfolio.RecordWork`의 공통 완료 정산으로
  옮겼다. NPC coordinator, 실제 플레이어 E 작업, 순수 시뮬레이션이 동일한 정산 경로를 사용한다.
- `Active → Completed` 전이 한 번에 현금·평판·기술을 처리한다. 완료 후 반복 호출/세이브 재로드로
  다시 지급하지 않는다. 실패·부분 작업에는 완료 기술 보상을 주지 않는다.
- 회사의 관련 기술 속도(최대 +20%)·품질(최대 +12)은 수락 시 고정한다. 실제 E 작업/NPC/코어 시간 검증이
  같은 인시당 분 계산을 사용한다. 신규 완료 품질도 저장하여 나중의 가족 성장으로 과거 품질이 바뀌지 않는다.
- 자체 개발/유지보수도 입문 두 하청의 DB·관리도구 기술 숙련을 사용해 속도/품질을 고정한다.
  기술을 재사용하는 효과와 완료 기술점을 다시 지급하는 행위는 구분한다.
- 자체 개발/유지보수는 `CompanyWorkPurpose`로 구분한다. 별도 완료 보상금·하청 평판·기술점수·대기업 진입용
  하청 완료 횟수를 만들어 내지 않는다. 개인의 실제 업무 XP는 계속 쌓인다.
- 업무 슬롯은 외부 하청과 내부 개발/지원 합계 2개다. 초기 반복 수입을 외부 하청 실적 농사로 사용할 수 없다.

## 구조와 저장

- 장기 소유자: `GameState.Growth.StarterProduct` (`StarterProductState`, 순수 C#).
- 작업: 기존 `ContractPortfolio` / `SubcontractState`를 이용한다. 내부 업무만 보상 0원 허용,
  일반 하청 `Accept`로 내부 업무를 우회 생성할 수 없다.
- 정산: `SimulationRunner`가 GameTime을 진행할 때 동기화. 벽시계/프레임/실제 로그인 간격으로 수입을 주지 않는다.
- UI: `ContractBusinessRuntimeAdapter`는 명령/결과 표시만 담당한다. 장기 상태나 기술 지급을 소유하지 않는다.
- 저장 **v12**: 제품 단계·시도 ID·품질·고객·만족도·정산 주기·수입·업무 목적/정확한 마감·고정 보너스·완료 품질.
  v1~v11을 읽는다. v11의 기존 기술점수/돈/계약은 보존하고 신규 제품 상태만 초기화한다.
  과거 누락 기술을 추측해 소급 지급하지 않으며, 기존 완료 기록은 입문 경험으로 인정한다.
- v12 저장을 v11 실행본으로 되돌려 읽는 것은 지원하지 않는다. 사용자 원본 세이브는 테스트하지 않고 보존한다.

## 이번 범위 밖

실제 기업의 고객 전용 소스/IP/상표를 하청 한 번으로 소유하게 하지 않는다. 입문 보상은 일반적인 DB·관리도구
업무 경험이며 동네 고객 의뢰는 게임 콘텐츠다. 특정 실존 기업의 실제 하청 이력을 새 역사 사실로 추가하지 않았다.

기존 4업종/기술표/계약 게시판/주식시장 코어/장기 사업 조건은 유지한다. 실제 시세 API 연결, 제품 실적에 따른
기업 가치·주가 반응, 라이선스 협상, 연구 트리 확장, 경쟁사·M&A는 이번 완료 항목이 아니다. 작은 입문 제품이
곧바로 대기업 주가를 움직이도록 연결하지 않는다. 다음 단계는 이 첫 반복 흐름의 플레이 밸런스 확인이다.

## 재검증 명령

```powershell
.\FAST_QA_WINDOWS.cmd -Profile simulation-pure
.\FAST_QA_WINDOWS.cmd -Profile editor-validation
.\FAST_QA_WINDOWS.cmd -Profile editor-broad
.\FAST_QA_WINDOWS.cmd -Profile player-scripts -NoPlayerSmoke
powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools/Background/Invoke-StarterProductQa.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools/Background/Invoke-StarterProductQa.ps1 -Lifecycle
```

순수 harness는 네 가족의 정산, 중복 방지, 시간 게이트, 입문 단계, 실제 기여가 없는 개발 정지,
시험 판매, 유지보수 성공/누락, 분할 시간 진행/저장 재개, v11 이관을 검증한다.
Unity에서는 동일 DTO를 실제 JsonUtility 직렬화로도 왕복한다.

Player QA는 기본적으로 별도 private desktop의 FastQA EXE를 실행한다. `-Player <exact Release EXE>`를
명시하면 같은 opt-in 검사를 그 배포 후보에서 수행한다. 유저 화면·입력·메인·세이브를 건드리지 않는다.
정상 새 게임에서 거래 API로 세트 4개 구매, 실제 UI raycast+managed pointer event로 입문 계약 수락/배정,
4배속 정상 GameTime과 실제 이동·착석을 거친 첫 4인시를 계측한다. Native pointer 구매 인증이나
전체 사업 주기를 정상 플레이로 관측했다는 증거로 확대 해석하지 않는다. 공개 배포 PASS와도 별개다.

`-Lifecycle`은 입문 기록/개발 앞 20인시를 코어 체크포인트로 구성한 뒤 실제 NPC의 마지막 개발 4인시와
지원 2인시를 확인한다. 주간 정산 시각만 이동하며 save는 메모리에서 왕복한다. 이번 공개 후보에서는
이 검사와 별도의 정상 4인 이동/출근/착석, 공개 전송 검사를 모두 통과했다. 중단 없는 전체 주간 native 플레이를 뜻하지 않는다.

## 실제 업무 연결에서 고친 문제

- 배정 후 이동·교통 정체·착석 준비 시간을 업무로 계산하지 않는다. 실제 `Working` 또는 도착/정렬 완료된
  비좌석 업무 중에만 배정 작업 시계가 감소한다. 4인시 테스트는 필요 착석 시간도 별도로 검증한다.
- 옆 타일의 캐릭터 몸이 경로를 막아 아빠가 멈추는 현상이 실제 Player에서 재현됐다. 배정 업무는 처음부터
  동적 우회 경로를 사용하고, 경로 탐색은 빈 칸 여부 외에 구간 전체의 몸체 여유를 검사한다. 정지한 상대의
  오래된 이동 의도만으로 우선권을 계속 보장하지 않는다. 타일 중앙 축·실제 충돌 반경·접촉 허용치는 유지한다.
  캐시/BFS/가중/비캐시 경로가 같은 조건을 사용한다. 겹침을 허용하거나 캐릭터를 숨겨 통과시키지 않는다.
- 경로의 마지막 짧은 구간도 일반 이동과 같은 축 제약을 사용한다. 충돌 투영이 옆으로 밀면 정지 후
  재경로를 요청한다. 실제 Release에서 검출한 0.006725칸 이탈을 수정했으며 기준 0.0001칸은 유지한다.
- 일정상 야간 수면 회복으로 다일 진행 시 전원 소진 후 영구 정지하던 문제를 해소했다.
  정수 GameTime 경계 계산은 1/2/4/17분 분할 및 중간 저장/복원에서 같은 값을 낸다.
