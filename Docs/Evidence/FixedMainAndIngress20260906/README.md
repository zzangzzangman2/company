# Fixed main entry / safe entrance follow-up

Workspace: `C:/Users/godho/Documents/Codex/fc_agents/integration_p0`, `main`.
Unity: `6000.3.21f1 (c02631ffc030)`. No public game Release or user main/save mutation.
Player source last observed: `68142d2df9aca9d09080c22461e54b0a8fe84a38`.

## Main entry and update evidence

- [MAIN_GAME_ENTRY.md](../../MAIN_GAME_ENTRY.md) owns home/company installation and the fixed EXE path.
- `latest-only-6-checks.json`: real production workers with injected API failures and a verified inert
  previous snapshot. All six reject startup without altering that snapshot. No executable/network run.
- `updater-51-checks.json`: file reuse, byte progress, integrity, path safety, cancellation and atomic
  activation. Snapshot integrity being valid does not authorize offline play.
- `remote-zero-inventory.json`: fresh remote branches/tags/full Release pagination. No public Releases,
  prohibited/unknown payloads zero. This is not evidence of game deployment.
- Existing actual local Unity transport/restart evidence is in
  [ChairTileCentre20260906/patch](../ChairTileCentre20260906/patch/). Its playable fixture was removed;
  those receipts do not certify this new source or an actual GitHub download.

## Gameplay results and their limits

- `22f0be5b-ingress-grid-handoff`: per-actor ingress allowed following, but a follower's rounded indoor
  cell blocked its leader. Actual attendance failed. Payload recycled, all hashes retained.
- `630350c7-next-day-timing`: distinct ingress/cell ownership removed that deadlock. Normal movement
  had no collisions and no long stall, but late Mother and 09:20 seating failed. Payload recycled.
- `normal-68142d2d`: actual normal opening walk, programmatic production shop purchase of four turns,
  original normal intents, normal departure, then 08:50–09:50 next-day observation. Only the unobserved
  afternoon/night was skipped. No actor control, route/pose injection, native pointer or user save.
- First visible releases: Player 09:00, older sister 09:01, Father 09:02, Mother 09:04. First Working:
  Player 09:33, older sister 09:24, Father 09:22, Mother 09:24. Exact scheduled entry/09:04 progress and
  the retained 09:20 diagnostic gate FAIL. Observing later work does not turn these into PASS.
- Independent navigation: 8,064 rows, max no-progress 1.79146 seconds, rail error 0.00002625,
  occupancy/runtime violations zero. This does not exclude inefficient detours or certify foot slip.
- Actual settled Working: 2,818 samples / all four identities and rotations, 20 complete positive-blend
  to seated transitions (0.3840–0.4339 seconds). Standard blend remains 0.42 seconds. Raw transition
  samples are retained. Mother/SouthWest has 111 settled individual-hand failures, max 0.032569 world
  against 0.015; other settled samples and knees pass. **Full seated pose is not PASS.**
- The last runner reported `Hidden-window guard failed` during shutdown and stopped its owned Player.
  The old runner wrote process.json only on normal exit, so it is absent. Do not fabricate an exit code
  or a full-run hidden-window PASS. Its complete failed attendance receipt was used for exact retirement;
  the identity manifest records process receipt absent and exit null. No subsequent Player launched.
- `next-day-normal-seated.png` is an offscreen engine camera-stack frame, not IMGUI/native input or
  continuous animation approval. Its filename is not a claim about four simultaneous workers.

`editor-red-green.json` identifies original logs by hash and retains the actual failure/PASS lines:
rounded-cell handoff and wrong leader/follower priority. The latter correction is **after** the latest
actual Player. Existing 128-seed / 1,152-path / shared locomotion regression also passes. Normal Player
verification of that source remains pending; no fresh executable was produced after the window guard.

All three failed 166-file payloads were recycled in full. Non-executable evidence is retained, source,
warm Library/Bee, saves, Downloads/main and unrelated builds preserved. No archived executable is here.

## Next safe steps

1. Resolve the shutdown-window detection without native input or foreground launches.
2. Verify the new leader-priority source through normal hidden attendance; keep failed due checks visible.
3. Diagnose Mother/SouthWest settled hand error separately from the permitted blend transition.
4. Obtain outstanding user visual/native-input acceptance before first Release and fixed-main installation.

Source/MD push is not a completed public patch, not a Downloads upgrade, and not approval to launch a
failed candidate. Production eligibility remains false.
