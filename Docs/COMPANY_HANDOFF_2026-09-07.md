# 회사 PC 인수인계 · 2026-09-07

## 먼저 실행할 게임

- 공개 패치: **fc-win-20260907.1 / sequence 4**.
- 게임 소스: **c0709823c0e45c4152c673ca0b67d7a1e1506bc7**. 이후 문서/evidence commit은 게임 빌드 SHA가 아니다.
- 메인: `%USERPROFILE%\Downloads\FamilyCompany_Playtest\FamilyCompany.exe`.
- 이미 설치했다면 같은 EXE를 연다. 게임 안에서 최신 공개 패치 확인 → 실제 수신 바이트 퍼센트 → 검증 → 재시작한다.
  플레이에 Unity나 git pull, 새 빌드는 필요 없다. 메인 EXE 하나만 복사하거나 날짜별 QA 실행본으로 교체하지 않는다.
- 처음 설치하는 회사 PC는 [전체 설치 ZIP](https://github.com/zzangzzangman2/company/releases/download/fc-win-20260907.1/FamilyCompany-Windows.zip)을
  위 폴더에 **전체** 압축 해제한다. 개인 파일이나 기존 설치가 있으면 무작정 덮어쓰지 않는다.
- 집 메인·사용자 패치 캐시(v2)·세이브 5개는 그대로 보존했다. 다음 실제 실행 때 패치 다운로드를 볼 수 있다.
  집/회사 세이브 자동 동기화는 없다. 최신 확인 실패 시 이전 게임을 실행하지 않는 정책이다.

## 개발을 이어받을 때

집에서 작업한 정본: `C:\Users\godho\Documents\Codex\fc_agents\integration_p0`, 브랜치 `main`.
예전 `2026-08-25\origin-main-340e45a6-clean-143-higgsfield` 작업 폴더가 아니다.
회사 체크아웃 위치는 달라도 되지만, 원격이 `zzangzzangman2/company`인지 먼저 확인한다.

```powershell
git status --short
git branch --show-current
git remote -v
# clean main임을 확인한 뒤에만:
git pull --ff-only origin main
```

변경이 있으면 삭제/강제복원/stash하지 말고 보존한다. 새 branch/worktree/하위 에이전트는 만들지 않는다.
읽는 순서: `AGENTS.md` → `Docs/PROJECT_STATE.md` → `Docs/STARTER_BUSINESS_LOOP.md` →
`Docs/MAIN_GAME_ENTRY.md` → 필요 시 `Docs/GITHUB_PATCHING.md`.
캐릭터 작업이면 먼저 `Docs/FAMILY_3D_CHARACTER_STANDARD.md`.
패키지 제작용 퍼블리셔는 **PowerShell 7.2 이상 (`pwsh`)**으로 실행한다. 게임의 패치 worker는 기존 Windows PowerShell 5.1을 계속 지원한다.
컴파일은 고정 Unity **6000.3.21f1**와 warm `FAST_QA_WINDOWS.cmd`; 릴리스 빌드는 최종 검증 배포 때만 한다.
사용자 회사 화면을 조작/전면 실행하지 않는다.

## 이번에 추가한 플레이

1. 새 게임: 빈 사무실, 500만 원, 가족 4명. 책상·PC·의자 세트는 40만 원이며 회전/배치/충돌 규칙은 유지.
2. `사업 → 자체 제품`에서 단어 DB 하청 → 대여점 관리 하청을 완료한다. 초기 상점의 PC 세트만으로 가능하다.
3. `우리가게 관리 프로그램` 개발: 30만 원, 24인시, 30일.
4. 시험 판매: 고객 3곳 × 사용권 6만 원 = 18만 원, 한 번만 지급.
5. 매주 유지보수 2인시 완료 → 고객당 2만 원. 미완료 주는 요금 0, 만족도 하락/고객 이탈.
   좋은 품질/만족도는 고객 증가로 이어진다. 500만/40만 외의 신규 가격은 테스트 밸런스다.

가족 업무 배정은 한 번에 최대 4인시이며 완료 후 다시 배정한다.
NPC는 실제 이동·착석·업무 상태에서만 기여한다. 플레이어 버튼은 대상 선택만 하므로
자기 책상 접근 칸으로 이동해 **E를 유지**한다. 달력만 넘겨 개발/지원 인시를 얻을 수 없다.
퇴근 후 일정상 23:00~07:00 수면은 8게임시간당 체력 한 바를 회복한다.
일반 대기나 실제 사무실 근무는 수면으로 보지 않으며 벽시계/오프라인 수입·회복 기능이 아니다.

공통 계약 완료 정산에서 NPC/플레이어 기술 보상을 정확히 한 번 지급한다.
내부 개발·유지보수는 기존 기술을 활용하되 하청 실적·기술·평판 보상을 다시 찍어내지 않는다.
외부/내부 업무는 활성 슬롯 2개를 공유한다.

## 저장·외형·다음 범위

- 저장 **v12**, v1~v11 읽기 호환. 기존 자산/기술/계약을 보존하며 과거 누락 보상을 추측 지급하지 않는다.
  v12 저장을 구 v11 실행본으로 되돌리는 것은 지원하지 않는다.
- 승인된 아들·아빠 모델 각 2명 구성 유지. 엄마/누나는 가족 identity만 별개이고 고유 3D 외형은 추후.
  캐릭터 크기, 책상/의자 축, 착석 자세를 이번 사업 패치에서 바꾸지 않았다.
- 실제 주식 시세 API, 자체 제품 실적→기업가치/주가, 실존 기업 라이선스 협상은 아직 구현하지 않았다.
  기존 시장 코어를 실시간 외부 시장 연결이라고 보고하지 않는다.
- 다음 우선순위는 이 첫 하청→제품→지원 루프의 사용자 플레이와 돈/시간 밸런스.
  그 뒤 연구/제품 계열 확장과 시장 연계 설계를 진행한다. 실존 고객의 전용 소스/IP를 자동 획득하는 설정은 아니다.

## 검증 증거와 한계

[이번 공개 배포 증거](Evidence/StarterBusinessRelease20260907/README.md)를 먼저 본다.
동일 Release의 실제 4인 보행·다음 날 출근·착석·가구 회피와 사업 작업 시간을 다시 측정했다.
사업의 긴 구간은 코어 체크포인트로 구성하고 마지막 개발 4인시/유지보수 2인시는 실제 NPC로 수행했다.
순수 전체 생명주기 테스트를 중단 없는 전체 주간 플레이로 표현하지 않는다.
상점 native pointer 증거는 변경 없는 구매/배치 코드에만 이전 승인 기록을 해시로 연결했고,
이번 검사를 새 Windows 마우스 클릭으로 표현하지 않는다.
GitHub 공개 패치 수신/파일 해시/100% 진행률은 별도 격리 root에서 검증했다.
사용자 캐시를 미리 업데이트하지 않았으므로 이번 사용자 메인에서 실제 Unity 재시작까지 실행했다고 주장하지 않는다.
